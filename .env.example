# Docker connection (used by make and CI container)
# Copy to .env and set values. .env is gitignored.
#
# Local Docker (default):
# DOCKER_HOST=unix:///var/run/docker.sock
#
# Remote Docker (TCP):
# DOCKER_HOST=tcp://your-docker-host:2375
#
# TLS (e.g. remote with certs):
# DOCKER_HOST=tcp://your-docker-host:2376
# DOCKER_TLS_VERIFY=1
# DOCKER_CERT_PATH=/path/to/certs

DOCKER_HOST=unix:///var/run/docker.sock

# Optional: registry login (token for docker pull/push from CI)
# If set, "docker login" is run before build so private bases or push work.
# DOCKER_REGISTRY=ghcr.io
# DOCKER_REGISTRY_USER=your-user-or-token-name
# DOCKER_REGISTRY_TOKEN=your-token-or-password
